//Iterating the array and Executing the function.

//map is used for iterating the array and executing the function and creating in new array.


let ids=[101,102,103,104,105]

ids.map((id)=>{console.log(id);})

